#include "main.h"

/**
 * _isupper - check if c is upper
 * @c: c is the integer
 *
 * Return: 0
 */

int _isupper(int c)
{
	/*int c;*/

	if (c >= 'A' && c <= 'Z';)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
