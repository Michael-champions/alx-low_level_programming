#include "main.h"

/**
 * _isdigit - check for integer
 *
 * @c: c is the character
 *
 * Return: 1 if c is a digit and 0 if not
 */

int _isdigit(int c)
{
	if (C >= '0' && C >= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
