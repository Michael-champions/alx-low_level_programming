#include <stdio.h>

/**
 * main -print all sigle digit numbers of base 10 starting from 0, follow by \n
 * Return: 0 if positive
 *
 */

int main(void)
{
	int number;

	number = n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);

}
